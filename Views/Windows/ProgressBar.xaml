<Window x:Class="FileEncryptor.Views.Windows.ProgressBar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:l="clr-namespace:FileEncryptor.Views.Windows"
        mc:Ignorable="d"
        Title="ProgressBar" Height="224" Width="479">
    
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Статус" Margin="5 20"/>
        
        <Grid Grid.Row="1" Margin="10 0 10 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1" Text="100%" HorizontalAlignment="Center" TextAlignment="Right" MinWidth="40" VerticalAlignment="Center" Margin="10 0"/>
            <ProgressBar Grid.Column="0" VerticalAlignment="Center" Height="20" IsIndeterminate="True"/>
            <Button Grid.Column="2" Content="Cancel" Padding="30 10" Margin="5" Background="WhiteSmoke"/>

        </Grid>
    </Grid>
</Window>
